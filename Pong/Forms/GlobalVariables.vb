Public Class GlobalVariables
    ' Used to tell another form that it should close.
    Public Shared closeForm As Boolean = False ' PauseMenu tells Game to close
    Public Shared closeProgram As Boolean = False ' PauseMenu tells Menu to close (ends the whole program)
End Class
